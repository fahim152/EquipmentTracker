# Docker Compose Environment Variables

# External port mappings (change if ports are already in use on your machine)
PULSAR_PORT=6689
PULSAR_ADMIN_PORT=8040
BACKEND_PORT=5156
FRONTEND_PORT=3050

# API URL for frontend (used during Docker build)
REACT_APP_API_URL=http://localhost:5156

# Pulsar configuration
PULSAR_MEM=-Xms512m -Xmx512m

